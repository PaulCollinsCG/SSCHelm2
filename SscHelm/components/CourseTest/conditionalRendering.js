
const ConditionalRendering = ({val}) => { 
    let format;
    // if(val > 100)
    //     format = className="text-primary"
    // return (
        <div format>{val}</div>
    // );
};

export default ConditionalRendering;